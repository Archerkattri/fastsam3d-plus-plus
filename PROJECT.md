# fastsam3d++

HiCache++ — DMD/Prony EXPONENTIAL velocity forecasting (new method) for Fast-SAM3D's slat flow-matching, with HiCache (Hermite) as the comparison baseline. The exponential basis is exact on the feature-ODE solution class where the polynomial drifts. See `hicache-plus-plus`.
